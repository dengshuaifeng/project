server:
  port: 18080
spring:
  application:
    name: pricesystem
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:
    username:
    password: root
  jackson:
    date-format: yyyy-MM-dd
    time-zone: GMT+8
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: cn.itcast.entity
  global-config:
    db-config:
      logic-not-delete-value: 0
      logic-delete-value: 1